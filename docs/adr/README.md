# Architecture Decision Records

Each ADR captures one decision, the context that forced it, and the consequences
accepted alongside it. ADRs are immutable references: they are amended in place with
a dated note rather than rewritten, and they are **never renumbered**, because their
numbers are cited from the Blueprint, the CHANGELOG and commit messages.

## Index

| ADR | Decision | Status | Implemented? |
|---|---|---|---|
| [002](ADR-002-spring-boot-bootstrap.md) | Spring Boot Walking Skeleton | Accepted (framework version superseded 2026-07-27) | Yes |
| [003](ADR-003-github-mcp-integration.md) | GitHub MCP integration | Accepted (amended 2026-07-28) | Yes |
| [004](ADR-004-api-contract-first.md) | API contract-first with OpenAPI 3.1 | Accepted | Yes — `docs/api/openapi.yml` hand-authored before the first controller |
| [005](ADR-005-error-handling-strategy.md) | Error handling via RFC 9457 Problem Details | Accepted | Yes — `GlobalExceptionHandler` maps every 4xx and 5xx |
| [006](ADR-006-build-quality-gates.md) | Build quality gates | Accepted | Yes |
| [007](ADR-007-versioning-strategy.md) | Semantic Versioning | Accepted | Yes |
| [008](ADR-008-database-migration-liquibase.md) | Database migrations with Liquibase | Accepted (amended 2026-08-10) | Yes — autoconfiguration fixed and the first changeset applies; the H2 console consequence is withdrawn |
| [009](ADR-009-frontend-react-typescript.md) | React + TypeScript + Vite + GOV.UK Frontend | Accepted | Yes — `ui/` scaffolded, built and served |
| [010](ADR-010-continuous-flow-over-sprints.md) | Continuous flow over sprint timeboxes | Accepted | Yes |
| [011](ADR-011-graphify-knowledge-graph.md) | Graphify knowledge graph via repo-local MCP server | Accepted | Yes |
| [012](ADR-012-deployment-target.md) | Deployment to a single EC2 instance with Terraform | Accepted (deployment target withdrawn 2026-08-10) | Partly — image and Compose run locally; Terraform validates but no `apply` has run |
| [013](ADR-013-feature-flags.md) | Feature flags via Spring configuration properties | Accepted | Yes — `eop.features.session-lifecycle` withholds `SessionController` via `@ConditionalOnProperty`; `SessionControllerDisabledIntegrationTest` asserts the bean is absent and all five routes 404. `eop.features.trick-play` does the same for `TrickController`, and `TrickPlayDisabledIntegrationTest` asserts its five use-case beans and the controller absent as well as all five trick-play routes answering 404. **Both flags are fail-closed as of EOP-48 (2026-08-14, commit `34d30d7`) and were not before:** the register's two 2026-08-14 notes record that a `@ConditionalOnProperty` with no `havingValue` reduces to `!"false".equalsIgnoreCase(value)`, so `off`, `no`, `0`, `disabled` and the **empty string** all enabled the feature; `SessionController` carried that form *and* left its four session use cases registered unconditionally, so the off position was a property of the URL space and not of the context. Both halves are closed — `havingValue = "true"` at `SessionController.java:60` plus the same condition on `createSessionUseCase`, `joinSessionUseCase`, `getSessionStateUseCase` and `startSessionUseCase` (`UseCaseConfiguration.java:101`, `:124`, `:167`, `:182`) — and `SessionLifecycleOffValueIntegrationTest` pins the `off` spelling with 12 tests that failed 6 of 12 against the pre-fix tree, `POST /api/v1/sessions` among them answering 201 Created. `resolvePlayerUseCase` stays ungated on purpose (pure read, shared with both flags; same precedent as `DeckShuffler`), and the ADR now mandates the explicit form **and** bean-level gating repository-wide |
| [014](ADR-014-realtime-transport.md) | Real-time transport via server-sent events | Accepted | Yes — `SseSessionEventPublisher` emits `player-joined` and `game-started`, and as of EOP-14 Slice E `hand-dealt`, `card-played` and `trick-resolved` from `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase`, each published after its write returns so a refused operation announces nothing; verified on a real socket through Caddy, `retry:3000` and `:heartbeat` frames included, events carrying no state |
| [015](ADR-015-player-identity.md) | Player identity via a server-issued opaque token in session storage | Accepted | Partly — the server half is done: 43-character token issued on admission, only its SHA-256 digest stored, no response or event carries the digest, and a missing credential is refused identically to a wrong one. The `sessionStorage` per-tab custody half is not built — `ui/` holds only a health-check shell and the string `sessionStorage` appears nowhere in it, so nothing yet keeps a token across a refresh. EOP-11 delivers it |
| [016](ADR-016-local-container-runtime.md) | Colima as the local container runtime | Accepted | Yes — installed, stack runs locally, all verification gates executed |
| [017](ADR-017-frontend-delivery-topology.md) | Front-end delivery via Caddy on a single origin | Accepted | Yes — proxy serves the site and forwards the API |
| [018](ADR-018-uuid-v7-identifiers.md) | UUID v7 primary keys generated through an application port | Accepted (narrowed 2026-08-12 — see ADR-023) | Yes — `IdentifierGenerator` mints identifiers in the use case, not at flush; `UuidV7IdentifierGeneratorTest` pins the version nibble, the variant and time ordering across 2,000 draws. **Narrowed to entity rows:** `trick_play_component` is keyed on `(trick_play_id, ordinal)` and `hand_card` on `(hand_id, card_id)`, and neither carries a UUID, because their rows are a value in a bounded list and a membership of a set rather than entities. Both tables exist in changeset `004` |
| [019](ADR-019-session-lifecycle-and-join-codes.md) | Session lifecycle, join codes, and header-only authentication on the event stream | Accepted (next-player formula amended 2026-08-12 — see ADR-023) | Yes — implemented across EOP-10: Crockford base32 codes, seat contention settled by `uq_player_session_seat`, throttled guessing, and one indistinguishable refusal for every unusable code. **Its next-player formula is superseded:** "leader's seat plus the number of cards already in the trick" holds only while every seat still holds a card, which EOP-14's whole-deck deal breaks on the last trick at four and five players. The general form is the next seat clockwise that still holds a card |
| [020](ADR-020-session-concurrency-control.md) | Concurrency control by compare-and-set on `status`, not by optimistic locking | Accepted (lock ordering decided 2026-08-12, corrected from a chain to a tree 2026-08-13 — see ADR-023; narrowed twice on 2026-08-14 by EOP-14 Slice E, which gave `current_leader_seat IS NULL` a third meaning and `sessionMoved` a sixth answer) | Yes — `touchWhileInStatus` and `advanceStatus` are status-guarded conditional updates whose rows-affected count is the protocol; `@Version` is mapped but deliberately unenforced, and no `OptimisticLockingFailureException` handler exists anywhere |
| [021](ADR-021-trusted-proxy-forwarded-for.md) | `X-Forwarded-For` trusted only from an explicit, default-denied allow-list of proxies | Accepted | Yes — `eop.web.trusted-proxies` defaults to the empty list, `ClientAddressResolver` ignores the header unless the peer is allow-listed, a malformed or non-canonical entry fails startup, and `ForwardedForThrottleBypassIntegrationTest` asserts both halves: the rotation attack is throttled, and the header is honoured when the peer is configured |
| [022](ADR-022-agent-model-tier-governance.md) | Model tiers allocated by Definition-of-Done role, not by artefact type | Accepted | Yes — `@tester-api` pinned to `{env:MODEL_B}`, all five DoD gates on `MODEL_A`/`MODEL_B`, and each carries a Sign-off Contract in its own definition. The narrowing of the Separation Invariant is recorded rather than denied, with both its exceptions named — test code, and production code the primary agent authors itself instead of delegating — and `AUDITOR_AGENTS`/`READ_ONLY_AGENTS` in `tools/agent-trace.py` are split so the write check no longer false-alarms on a tester writing a test |
| [023](ADR-023-deal-remainder-and-turn-order.md) | Every card is dealt with the remainder going to seats in ascending order, the current leader's seat stored on `game_session`, and `TrickPlay.components` stored one row per component in `trick_play_component` | Accepted | Partly — the domain half of decision 1 shipped in EOP-14 Slice A: `Hands.deal` round-robins the whole deck so the remainder falls on the lowest seats, `Hands.openingLeaderSeat` derives the opening lead with no rank written down, and `Trick.nextLeaderSeat(seatsHoldingCards)` passes the lead on when the winner has run out of cards. Decision 2 is **built and now reachable** as of EOP-14 Slice C1: `current_leader_seat` landed in Liquibase changeset `004`, and `HandRepository.findCurrentLeaderSeat`, `HandRepository.recordDeal` and `TrickRepository.recordResolution` now read and advance it through `TrickPlayRepositoryAdapter`; Slice C2 now calls them from `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase`, which derive the leader seat from a read and never from a request body; and as of EOP-14 Slice D `TrickController` reaches all four of those beans over HTTP, gated on the same flag, with Slice E adding a fifth, `GetTrickStateUseCase`, behind a fifth route on the same controller and the same flag. EOP-14 Slice E discharges the last of the deferral: `ResolveTrickUseCase` passes `Trick.nextLeaderSeat(seatsHoldingCards)` straight through to `TrickRepository.recordResolution`, whose next-leader parameter widened from `int` to `OptionalInt`, so a played-out hand records `current_leader_seat = NULL` rather than a winning seat with nothing left to lead, and `GET /api/v1/sessions/{sessionId}/tricks/current` publishes `seatToPlay`, `complete`, `nextLeaderSeat` and `handComplete` so a client can learn whose turn it is without attempting a play and being refused. The components decision, added by the 2026-08-12 amendment, is likewise **built and now mapped**: the domain layer of its 20-element bound shipped in Slice A, `trick_play_component` with `PRIMARY KEY (trick_play_id, ordinal)` and `CHECK (ordinal <= 19)` ships in changeset `004` with migration tests pinning the CHECK to `TrickPlay.MAX_COMPONENTS`, and the `@Size(max = 20)` boundary layer shipped in Slice D at `PlayCardRequest.java:53-54`, pinned to `TrickPlay.MAX_COMPONENTS` rather than to a literal, with a per-element bound and an HTTP test for both sides of it; `TrickPlayComponentJpaEntity` maps the table as of Slice C1, and the adapter writes ordinals it computes itself rather than trusting a payload. Slice B also creates `hand`, `hand_card`, `trick`, `trick_play` and `trick_play_component` with 6 unique constraints and 10 foreign keys; `hand_card` keyed on `(hand_id, card_id)` is what makes the `20, 20, 19, 19` deal storable at all and was missing from this ADR's own changeset inventory until the 2026-08-12 amendment. **Seat binding *is* in storage**, reversing an earlier deferral on the same day: `uq_player_id_seat` on `player (id, seat_order)` plus `fk_hand_player_seat` and `fk_trick_play_player_seat` make a hand or a play at a seat its player does not hold unrepresentable, and the single-column keys to `player(id)` are subsumed rather than omitted. **Cross-session enforcement remains deliberately not in storage**, and Slice C2 did not put it there — it ships no Liquibase changeset, so as far as the database is concerned the play path, the deal path (`hand` has the identical gap) and the delete path all still accept a row naming a player, a seat or a card that belongs to another session. What Slice C2 did instead is make its own three paths unable to *construct* such a row: every identifier a write carries is derived from a read scoped to the session the caller was resolved in — `PlayCardUseCase` takes the acting seat and player from the resolved player and the hand from that session's `Hands`, and `DealHandsUseCase` deals only to the seats the resolved session reports — so a cross-session play is refused above storage rather than by it. That is narrower than the obligation and should be read as narrower: it protects the three paths this slice wrote, not the five tables, and the first future path that reaches these ports without repeating the discipline is unguarded again. Audit then found a **fourth** gap that is not cross-session at all: `fk_trick_winner_play` confines a trick's winner to no trick and no session. The ADR names **four** Slice C obligations, each naming its exception type and the HTTP status the refusal carries — 403/404 for the seat and membership checks, 409 for the duplicate constraints, 422 for the wrong-trick winner, and a fourth obligation to extend the adapters' constraint-name translation so those violations stop answering 500 (@tester-api, 2026-08-12). **Obligation 4 shipped in Slice C1** for every row of its table except the two seat foreign keys, which the adapter deliberately treats as backstops and rethrows as server faults because it refuses a wrong seat *above* the constraint; that row is superseded in place. **Obligation 1 is partly discharged, and not in the layer it names**: `assertSeated` throws both `NotYourSeatException` and `PlayerNotInSessionException` with the body parity the obligation requires, but that is a check on the *row* — does the player a row names hold the seat it claims — and not on the requester, so deriving the acting player from authenticated identity and refusing a caller-supplied seat are discharged by Slice C2, whose three use cases resolve the caller first and derive the acting seat from the resolved player, and whose `PlayCardCommand` cannot carry a seat at all. Changeset `005` added range CHECKs to `player.seat_order`, `trick.sequence` and `game_session.current_leader_seat`, pinned to `GameSession.MAXIMUM_PLAYERS`. Slice C1 also **mapped all five tables** — `@Table` now appears on eight classes, not three — so they are reachable through `HandRepository` and `TrickRepository` by any bean that injects either port, and so is every deferral above. Containment is no longer the absence of a use case, because Slice C2 wrote the callers: what contains the deferrals today is `eop.features.trick-play`, `matchIfMissing` left at false, in front of five use-case beans and `TrickController` (`UseCaseConfiguration.java:198-308`, `application.yml:99`); Slice D wrote the route and gated it on that same flag with `havingValue = "true"`, and an off-position test asserts all six beans absent as well as all five routes answering 404. A flag is weaker than a missing class, because it is one property away from live rather than one commit. See [ADR-024](ADR-024-trick-play-persistence-boundary.md) for the boundary those two ports draw, and the reversal and the remaining deferrals in the ADR |
| [024](ADR-024-trick-play-persistence-boundary.md) | One adapter implements both trick-play ports, and neither port authorises anybody | Accepted | Yes — `TrickPlayRepositoryAdapter` implements `HandRepository` and `TrickRepository` as one class in EOP-14 Slice C1, because playing a card deletes a `hand_card` row and inserts a `trick_play` row with its components and those writes must commit together; the ports stay separate because their callers are separate. Neither port takes an acting player, so no implementation of them can authorise a requester: `recordDeal` and `appendPlay` check the row, `openTrick` and `recordResolution` check nothing, and the three reads have no parameter to check against. Authorising the requester is the use case's obligation and is **discharged as of EOP-14 Slice C2**: `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase` each resolve the caller through `ResolvePlayerUseCase` as their first statement, and `PlayCardCommand` carries no seat, no player identifier, no suit and no rank, so a caller cannot name a seat at all. No port can enforce this and no C1 test failed for its absence, which is why it was recorded here rather than only in javadoc; what holds it now is a test per use case asserting that a stranger is refused **before any hand is read**. The oracle described above is therefore reachable only by a member, and only through beans that do not exist while `eop.features.trick-play` is false |
| [025](ADR-025-dealing-is-its-own-use-case.md) | Dealing is its own use case rather than a step in starting a session, and the deal follows the start write across a seam the ADR argues is chosen rather than forced | Accepted (decided 2026-08-13; both handoffs it deferred discharged 2026-08-14 — Slice D minted the three event names, Slice E published them and deleted the `nextLeaderSeat` placeholder) | Yes — EOP-14 Slice C2 ships `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase` as plain classes in `org.maglez.eop.usecase`, wired in `UseCaseConfiguration` behind `@ConditionalOnProperty` on `eop.features.trick-play`, which `application.yml` defaults to false; `StartSessionUseCase` is untouched and still deals nothing. Authorisation is the first port call of all three, `DealHandsUseCase` returns `void` and refuses a participant with `NotFacilitatorException` and a short table with `TooFewPlayersException` before the deck is even read, and `ResolveTrickUseCase` admits any seated member rather than the facilitator alone. The `DeckShuffler` port with `SecureRandomDeckShuffler` supplies the shuffle `Hands.deal` deliberately omits, and `CardRepository.findWholeDeck()` returns the whole deck in canonical order rather than a page. `TrickPlayDisabledIntegrationTest` asserts all three beans are absent with the flag off while the ungated shuffler remains. Two things were deliberately not in Slice C2: no route reached these beans, so the started-but-undealt window of decision 2 was not yet recoverable by a client, and recognising the end of a hand — where `ResolveTrickUseCase` currently writes the winning seat as a placeholder next leader — is Slice E's. EOP-14 Slice D closed the first of the two: `TrickController` now reaches all three beans plus the eleventh use case `ReadOwnHandUseCase`, so a facilitator can deal and a player can read their own hand. The second is closed by EOP-14 Slice E: the placeholder is gone, `TrickRepository.recordResolution` takes an `OptionalInt` next leader and records no leading seat once no seat holds a card, `HandCompleteException` refuses a play into a spent hand with its own 409 rather than the 422 an empty hand would otherwise produce, and `handComplete` on the new state-of-play read says the hand is over without claiming the score is final |
| [026](ADR-026-use-case-observability.md) | Where use case observability lives is an open question, and no use case logs anything | Proposed | No — deliberately. This ADR records an obligation rather than discharging one. `.opencode/rules/observability.md` requires `INFO` at use case operation boundaries and audit logging for game-affecting actions, and none of the twelve use cases logs: `CreateSession`, `DealHands`, `GetCard`, `GetSessionState`, `GetTrickState`, `JoinSession`, `ListCards`, `PlayCard`, `ReadOwnHand`, `ResolvePlayer`, `ResolveTrick`, `StartSession`. Logging exists only in the web adapter, where `GlobalExceptionHandler` records server faults. Three gates raised this against EOP-14 Slice C2 — @tester-unit-and-quality, @code-reviewer and @architecture-guardian — and all three judged it repository-wide rather than that slice's defect, since its three use cases are exactly as compliant as the seven before them and fixing only those three would make the codebase less uniform. The ADR is `Proposed` because the obvious implementation is the wrong one: an SLF4J logger in a use case would put a framework import into the layer `clean-architecture.md` requires to have none, which is why `UseCaseConfiguration` exists. It sets out four arrangements that survive that constraint and three questions any of them must answer — CWE-117 forging on player-supplied `notes` and `components`, not logging card identities out of private hands, and where an `X-Correlation-Id` may be trusted from given ADR-021 pins `forward-headers-strategy` to `none`. Nothing enforces it and the build is green without it, so it needs its own story — and EOP-14 Slice D raised the stakes without discharging it: Slice D added the eleventh use case and `TrickController`, the first routes through which dealing, playing and resolving can be invoked at all, so those three writes are now reachable *and* unobserved. @security-auditor made resolving this ADR a **predecessor** of the story that turns `eop.features.trick-play` on, not a parallel backlog item. EOP-14 Slice E added the twelfth use case and a fifth route and still logs nothing, and [ADR-028](ADR-028-end-of-hand-without-release-or-score.md) names this ADR as the first of that story's three predecessors, alongside EOP-48 and EOP-15 |
| [027](ADR-027-singleton-subresource-naming.md) | A singleton sub-resource may be singular, and `/hands` is forbidden rather than merely unused | Accepted | Yes — EOP-14 Slice D ships `GET /api/v1/sessions/{sessionId}/hand`, singular, departing from `api-design.md`'s binding plural-noun rule because the path resolves its subject entirely from the identity token and takes no selector, so it is a singleton by construction rather than a collection that happens to hold one member. The substantive half of the decision is the prohibition, not the naming: no route, use case, port method or DTO may return a hand the caller does not hold, so there is no `HandsDto`, no `HandPage` and no `/hands`. Nothing mechanically enforces that — the way to enforce "this route must never exist" is a review — but adding one would need a new port method or use case, both conspicuous in a diff. `api-design.md` is unamended and plural remains the default; this is a named exception with a stated test. Recorded because @architecture-guardian ruled during the Slice D review that a javadoc is invisible to the next agent, who reads the rule file rather than the controller |
| [028](ADR-028-end-of-hand-without-release-or-score.md) | The end of a hand is reported, not released and not scored | Accepted | Yes
| [029](ADR-029-global-rule-loading.md) | All rule files loaded globally via a single glob | Accepted | Yes — `.opencode/opencode.json` `instructions` is `[".opencode/rules/*.md"]`; nine `## Required Reading` sections deleted from agent definitions (EOP-000) — EOP-14 Slice E reports the end of a hand in two derived places and stores it in neither: `current_leader_seat` becomes `NULL` once no seat holds a card, and `handComplete` on `GET /api/v1/sessions/{sessionId}/tricks/current` is computed from `Hands.allEmpty()`. The session status deliberately stays `IN_PROGRESS`, because `SessionStatus.COMPLETED` is documented as a hand whose score is final and EOP-14 computes no score; EOP-15 owns that transition, and a client watching only `GET /api/v1/sessions/{sessionId}` therefore never sees a session finish. The second decision is a deliberate non-change: `eop.features.trick-play` stays `false` on merge, so all five slices ship unreleased and turning the flag on is its own story whose predecessors are [ADR-026](ADR-026-use-case-observability.md), EOP-48 and EOP-15 |

The "Implemented?" column exists because an accepted ADR is a decision, not a
delivery. `CHANGELOG.md` separates the same two things for the same reason.

## Why numbering starts at 002

There is no ADR-001. The first architectural decision — adopting Spring Boot as the
application framework — was recorded as ADR-002 and 001 was never written. Rather
than renumber a set of documents that other files reference by number, the gap is
recorded here and left alone.

## Adding an ADR

1. Take the next free number. Do not reuse a number, even for a superseded ADR.
2. Name the file `ADR-NNN-kebab-case-title.md`.
3. Follow the house structure: `# ADR-NNN: Title`, then `**Status:**`, `**Date:**`
   and `**Deciders:**`, then `## Context`, `## Decision`, `## Consequences`,
   `## Related`.
4. State consequences honestly, including the negative ones. An ADR that lists only
   benefits has not recorded a decision, only an advertisement.
5. Add a row to the index above, and keep its Status cell in step with the ADR's own
   `**Status:**` line — including the date whenever an amendment changes the status.
   An amendment that leaves the status word alone, such as withdrawing a consequence
   that was never true, needs no date in either place; that is why several ADRs here
   record a dated amendment — some under a `## Amendments` heading, one as an inline
   `**Amendment, YYYY-MM-DD …**` paragraph — above a bare `Accepted`.
   `AdrIndexConsistencyTest` fails the build if a file has no row, a row points at no
   file, a file carries anything other than exactly one `**Status:**` line, the leading
   status word disagrees, or a dated status line reaches the index without its date.
   It checks that last rule in one direction only — an undated status line is always
   accepted. That test exists because this step was drifted past twice.
6. To reverse a decision, write a new ADR that supersedes the old one and mark the
   old one `Superseded by ADR-NNN`. Do not edit the original's decision.
