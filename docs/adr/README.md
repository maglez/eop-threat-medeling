# Architecture Decision Records

Each ADR captures one decision, the context that forced it, and the consequences
accepted alongside it. ADRs are immutable references: they are amended in place with
a dated note rather than rewritten, and they are **never renumbered**, because their
numbers are cited from the Blueprint, the CHANGELOG and commit messages.

## Index

| ADR | Decision | Status | Implemented? |
|---|---|---|---|
| [002](ADR-002-spring-boot-bootstrap.md) | Spring Boot Walking Skeleton | Accepted (framework version superseded 2026-07-27) | Yes |
| [003](ADR-003-github-mcp-integration.md) | GitHub MCP integration | Accepted (amended 2026-07-28) | Yes |
| [004](ADR-004-api-contract-first.md) | API contract-first with OpenAPI 3.1 | Accepted (amended 2026-08-19) | Yes — `docs/api/openapi.yml` hand-authored before the first controller |
| [005](ADR-005-error-handling-strategy.md) | Error handling via RFC 9457 Problem Details | Accepted | Yes — `GlobalExceptionHandler` maps every 4xx and 5xx |
| [006](ADR-006-build-quality-gates.md) | Build quality gates | Accepted (amended 2026-08-18, 2026-08-19, 2026-08-20) | Yes |
| [007](ADR-007-versioning-strategy.md) | Semantic Versioning | Accepted | Yes |
| [008](ADR-008-database-migration-liquibase.md) | Database migrations with Liquibase | Accepted (amended 2026-08-10, 2026-08-19) | Yes — autoconfiguration fixed and the first changeset applies; the H2 console consequence is withdrawn and the Context labels row is glossed by ADR-043 |
| [009](ADR-009-frontend-react-typescript.md) | React + TypeScript + Vite + GOV.UK Frontend | Accepted (amended 2026-08-19, 2026-08-20) | Yes — `ui/` scaffolded, built and served |
| [010](ADR-010-continuous-flow-over-sprints.md) | Continuous flow over sprint timeboxes | Accepted | Yes |
| [011](ADR-011-graphify-knowledge-graph.md) | Graphify knowledge graph via repo-local MCP server | Accepted | Yes |
| [012](ADR-012-deployment-target.md) | Deployment to a single EC2 instance with Terraform | Accepted (deployment target withdrawn 2026-08-10) | Partly — image and Compose run locally; Terraform validates but no `apply` has run |
| [013](ADR-013-feature-flags.md) | Feature flags via Spring configuration properties | Accepted | Yes — `eop.features.session-lifecycle` withholds `SessionController` via `@ConditionalOnProperty`; `SessionControllerDisabledIntegrationTest` asserts the bean is absent and all five routes 404. `eop.features.trick-play` does the same for `TrickController`, `ScoreController` and `EndSessionController`, and `TrickPlayDisabledIntegrationTest` asserts its seven use-case beans and all three controllers absent — ten beans in all — as well as all seven trick-play routes answering 404 (re-derived 2026-08-21, EOP-49; this row said six use-case beans, two controllers, eight beans and six routes until then, and unlike ADR-013's own paragraph it was never dated, so it read as current while going stale — the flag's live value and arity are ADR-013's to state, and regeneration commands live there). **Both flags are fail-closed as of EOP-48 (2026-08-14, commit `34d30d7`) and were not before:** the register's two 2026-08-14 notes record that a `@ConditionalOnProperty` with no `havingValue` reduces to `!"false".equalsIgnoreCase(value)`, so `off`, `no`, `0`, `disabled` and the **empty string** all enabled the feature; `SessionController` carried that form *and* left its four session use cases registered unconditionally, so the off position was a property of the URL space and not of the context. Both halves are closed — `havingValue = "true"` at `SessionController.java:60` plus the same condition on `createSessionUseCase`, `joinSessionUseCase`, `getSessionStateUseCase` and `startSessionUseCase` (`UseCaseConfiguration.java:101`, `:124`, `:167`, `:182`) — and `SessionLifecycleOffValueIntegrationTest` pins the `off` spelling with 12 tests that failed 6 of 12 against the pre-fix tree, `POST /api/v1/sessions` among them answering 201 Created. `resolvePlayerUseCase` stays ungated on purpose (pure read, shared with both flags; same precedent as `DeckShuffler`), and the ADR now mandates the explicit form **and** bean-level gating repository-wide |
| [014](ADR-014-realtime-transport.md) | Real-time transport via server-sent events | Accepted | Yes — `SseSessionEventPublisher` emits `player-joined` and `game-started`, and as of EOP-14 Slice E `hand-dealt`, `card-played` and `trick-resolved` from `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase`, each published after its write returns so a refused operation announces nothing; events carry no state. **The through-Caddy verification is a dated manual observation, not a check the build repeats.** On 2026-08-05, under EOP-10 (commit `87d8c49`), the stream was read on a real socket through Caddy with `retry:3000` and `:heartbeat` frames observed — covering the two events that existed then, `player-joined` and `game-started`, and no others. The three EOP-14 Slice E events were folded into this sentence on 2026-08-14 and were **never** part of that observation. Nothing reproduces it: `SessionStreamIntegrationTest` exercises the stream against the application directly, and ADR-035 records that no test in the suite goes through Caddy (the k6 smoke test of ADR-017 does, but not on the SSE path). Treat the frame-level behaviour of the proxy as observed once, on two of the five events — re-observe it or script it rather than citing this row as evidence |
| [015](ADR-015-player-identity.md) | Player identity via a server-issued opaque token in session storage | Accepted | Mostly — the server half is done: 43-character token issued on admission, only its SHA-256 digest stored, no response or event carries the digest, and a missing credential is refused identically to a wrong one. EOP-11 added the client half: custody in `sessionStorage` under `eop_session`, replay via the `X-EoP-Player-Token` header, and `fetch`-based SSE reading that closes this ADR's `EventSource` open question without resorting to a query parameter. One gap remains — the client's recovery from an expired or deleted session matches on the strings `403`/`404` in a problem `detail` that never contains them, so a stale token wedges the tab instead of returning it home (EOP-11) |
| [016](ADR-016-local-container-runtime.md) | Colima as the local container runtime | Accepted | Yes — installed, stack runs locally, all verification gates executed |
| [017](ADR-017-frontend-delivery-topology.md) | Front-end delivery via Caddy on a single origin | Accepted | Yes — proxy serves the site and forwards the API |
| [018](ADR-018-uuid-v7-identifiers.md) | UUID v7 primary keys generated through an application port | Accepted (narrowed 2026-08-12 — see ADR-023) | Yes — `IdentifierGenerator` mints identifiers in the use case, not at flush; `UuidV7IdentifierGeneratorTest` pins the version nibble, the variant and time ordering across 2,000 draws. **Narrowed to entity rows:** `trick_play_component` is keyed on `(trick_play_id, ordinal)` and `hand_card` on `(hand_id, card_id)`, and neither carries a UUID, because their rows are a value in a bounded list and a membership of a set rather than entities. Both tables exist in changeset `004` |
| [019](ADR-019-session-lifecycle-and-join-codes.md) | Session lifecycle, join codes, and header-only authentication on the event stream | Accepted (next-player formula amended 2026-08-12 — see ADR-023; join code widened to eight characters 2026-08-22 — see EOP-24) | Yes — implemented across EOP-10: Crockford base32 codes, seat contention settled by `uq_player_session_seat`, throttled guessing, and one indistinguishable refusal for every unusable code. **Its next-player formula is superseded:** "leader's seat plus the number of cards already in the trick" holds only while every seat still holds a card, which EOP-14's whole-deck deal breaks on the last trick at the three table sizes whose deal is uneven (68 cards do not divide evenly at 3, 5 or 6 players, though they do at 4). The general form is the next seat clockwise that still holds a card. **Deck size further amended 2026-08-18 (EOP-75):** the deck is now 68 cards — see [ADR-041](ADR-041-printed-deck-has-no-aces.md). **Join code widened 2026-08-22 (EOP-24):** codes are eight Crockford characters, exactly forty bits, because thirty bits was enumerable by a distributed attacker in days rather than the centuries the original comment claimed — the limiter is now one of two controls rather than the primary one |
| [020](ADR-020-session-concurrency-control.md) | Concurrency control by compare-and-set on `status`, not by optimistic locking | Accepted (lock ordering decided 2026-08-12, corrected from a chain to a tree 2026-08-13 — see ADR-023; narrowed twice on 2026-08-14 by EOP-14 Slice E, which gave `current_leader_seat IS NULL` a third meaning and `sessionMoved` a sixth answer) | Yes — `touchWhileInStatus` and `advanceStatus` are status-guarded conditional updates whose rows-affected count is the protocol; `@Version` is mapped but deliberately unenforced, and no `OptimisticLockingFailureException` handler exists anywhere |
| [021](ADR-021-trusted-proxy-forwarded-for.md) | `X-Forwarded-For` trusted only from an explicit, default-denied allow-list of proxies | Accepted | Yes — `eop.web.trusted-proxies` defaults to the empty list, `ClientAddressResolver` ignores the header unless the peer is allow-listed, a malformed or non-canonical entry fails startup, and `ForwardedForThrottleBypassIntegrationTest` asserts both halves: the rotation attack is throttled, and the header is honoured when the peer is configured |
| [022](ADR-022-agent-model-tier-governance.md) | Model tiers allocated by Definition-of-Done role, not by artefact type | Accepted (rule 1 superseded 2026-08-21 by ADR-046) | Yes — `@tester-api` pinned to `{env:MODEL_B}`, every DoD gate on a reasoning tier, and each carries a Sign-off Contract in its own definition. This cell read "all five DoD gates on `MODEL_A`/`MODEL_B`" until 2026-08-21, when ADR-046 moved `@architecture-guardian` and `@security-auditor` to `MODEL_F` and replaced rule 1's tier-name whitelist with a capability floor plus a mandatory probe; rules 2 to 6 stand. The narrowing of the Separation Invariant is recorded rather than denied, and rule 5's second exception — production code the primary agent authors itself instead of delegating — is now closed, leaving one: test code. The diagram in the Decision section is the 2026-08-11 arrangement, retained as history. `AUDITOR_AGENTS`/`READ_ONLY_AGENTS` in `tools/agent-trace.py` are split so the write check no longer false-alarms on a tester writing a test |
| [023](ADR-023-deal-remainder-and-turn-order.md) | Every card is dealt with the remainder going to seats in ascending order, the current leader's seat stored on `game_session`, and `TrickPlay.components` stored one row per component in `trick_play_component` | Accepted (decision 1 superseded 2026-08-17 by EOP-72, restored 2026-08-18 by EOP-92) | **Decision 1 reads correctly again as of EOP-92 (2026-08-18), after a spell in which this summary was stale.** EOP-72 replaced the full deal with equal hands and a discarded remainder, and this row was not amended to say so — it went on describing the whole-deck deal for one story while the code discarded two to four cards per game. EOP-92 reverts EOP-72 at the product owner's request: `Hands.deal` deals every card again, so 68 cards go out as 23/23/22, 17/17/17/17, 14/14/14/13/13 or 12/12/11/11/11/11 in seat order with nothing discarded, `Hands.openingLeaderSeat` is total by construction rather than by a swap that always landed the lead on the last seat, and the short final trick is exercised rather than merely supported at 3, 5 and 6 players (four players is the exception — 68 divides evenly by four, so hands are equal there). Conservation is pinned by `HandsTest.shouldDiscardNothingAtAnyTableSize` across all four table sizes; this also resolves divergence 1 of EOP-83 in favour of `docs/api/openapi.yml`, which had documented the full deal all along. Partly — the domain half of decision 1 shipped in EOP-14 Slice A: `Hands.deal` round-robins the whole deck so the remainder falls on the lowest seats, `Hands.openingLeaderSeat` derives the opening lead with no rank written down, and `Trick.nextLeaderSeat(seatsHoldingCards)` passes the lead on when the winner has run out of cards. Decision 2 is **built and now reachable** as of EOP-14 Slice C1: `current_leader_seat` landed in Liquibase changeset `004`, and `HandRepository.findCurrentLeaderSeat`, `HandRepository.recordDeal` and `TrickRepository.recordResolution` now read and advance it through `TrickPlayRepositoryAdapter`; Slice C2 now calls them from `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase`, which derive the leader seat from a read and never from a request body; and as of EOP-14 Slice D `TrickController` reaches all four of those beans over HTTP, gated on the same flag, with Slice E adding a fifth, `GetTrickStateUseCase`, behind a fifth route on the same controller and the same flag. EOP-14 Slice E discharges the last of the deferral: `ResolveTrickUseCase` passes `Trick.nextLeaderSeat(seatsHoldingCards)` straight through to `TrickRepository.recordResolution`, whose next-leader parameter widened from `int` to `OptionalInt`, so a played-out hand records `current_leader_seat = NULL` rather than a winning seat with nothing left to lead, and `GET /api/v1/sessions/{sessionId}/tricks/current` publishes `seatToPlay`, `complete`, `nextLeaderSeat` and `handComplete` so a client can learn whose turn it is without attempting a play and being refused. The components decision, added by the 2026-08-12 amendment, is likewise **built and now mapped**: the domain layer of its 20-element bound shipped in Slice A, `trick_play_component` with `PRIMARY KEY (trick_play_id, ordinal)` and `CHECK (ordinal <= 19)` ships in changeset `004` with migration tests pinning the CHECK to `TrickPlay.MAX_COMPONENTS`, and the `@Size(max = 20)` boundary layer shipped in Slice D at `PlayCardRequest.java:53-54`, pinned to `TrickPlay.MAX_COMPONENTS` rather than to a literal, with a per-element bound and an HTTP test for both sides of it; `TrickPlayComponentJpaEntity` maps the table as of Slice C1, and the adapter writes ordinals it computes itself rather than trusting a payload. Slice B also creates `hand`, `hand_card`, `trick`, `trick_play` and `trick_play_component` with 6 unique constraints and 10 foreign keys; `hand_card` keyed on `(hand_id, card_id)` is what makes the `20, 20, 19, 19` deal storable at all and was missing from this ADR's own changeset inventory until the 2026-08-12 amendment. **Seat binding *is* in storage**, reversing an earlier deferral on the same day: `uq_player_id_seat` on `player (id, seat_order)` plus `fk_hand_player_seat` and `fk_trick_play_player_seat` make a hand or a play at a seat its player does not hold unrepresentable, and the single-column keys to `player(id)` are subsumed rather than omitted. **Cross-session enforcement remains deliberately not in storage**, and Slice C2 did not put it there — it ships no Liquibase changeset, so as far as the database is concerned the play path, the deal path (`hand` has the identical gap) and the delete path all still accept a row naming a player, a seat or a card that belongs to another session. What Slice C2 did instead is make its own three paths unable to *construct* such a row: every identifier a write carries is derived from a read scoped to the session the caller was resolved in — `PlayCardUseCase` takes the acting seat and player from the resolved player and the hand from that session's `Hands`, and `DealHandsUseCase` deals only to the seats the resolved session reports — so a cross-session play is refused above storage rather than by it. That is narrower than the obligation and should be read as narrower: it protects the three paths this slice wrote, not the five tables, and the first future path that reaches these ports without repeating the discipline is unguarded again. Audit then found a **fourth** gap that is not cross-session at all: `fk_trick_winner_play` confines a trick's winner to no trick and no session. The ADR names **four** Slice C obligations, each naming its exception type and the HTTP status the refusal carries — 403/404 for the seat and membership checks, 409 for the duplicate constraints, 422 for the wrong-trick winner, and a fourth obligation to extend the adapters' constraint-name translation so those violations stop answering 500 (@tester-api, 2026-08-12). **Obligation 4 shipped in Slice C1** for every row of its table except the two seat foreign keys, which the adapter deliberately treats as backstops and rethrows as server faults because it refuses a wrong seat *above* the constraint; that row is superseded in place. **Obligation 1 is partly discharged, and not in the layer it names**: `assertSeated` throws both `NotYourSeatException` and `PlayerNotInSessionException` with the body parity the obligation requires, but that is a check on the *row* — does the player a row names hold the seat it claims — and not on the requester, so deriving the acting player from authenticated identity and refusing a caller-supplied seat are discharged by Slice C2, whose three use cases resolve the caller first and derive the acting seat from the resolved player, and whose `PlayCardCommand` cannot carry a seat at all. Changeset `005` added range CHECKs to `player.seat_order`, `trick.sequence` and `game_session.current_leader_seat`, pinned to `GameSession.MAXIMUM_PLAYERS`. Slice C1 also **mapped all five tables** — `@Table` now appears on eight classes, not three — so they are reachable through `HandRepository` and `TrickRepository` by any bean that injects either port, and so is every deferral above. Containment is no longer the absence of a use case, because Slice C2 wrote the callers: what contained the deferrals at Slice D was `eop.features.trick-play`, in front of six use-case beans and both `TrickController` and `ScoreController`; Slice D wrote the route and gated it on that same flag with `havingValue = "true"`, and an off-position test asserted the beans absent and the routes answering 404. A flag is weaker than a missing class, because it is one property away from live rather than one commit — **and that property was changed: EOP-70 flipped the flag `true` on 2026-08-17, so nothing on this list is contained any longer.** Every deferral above is reachable through a live route, and only the authorisation in [ADR-024](ADR-024-trick-play-persistence-boundary.md) stands in front of it (noted 2026-08-21, EOP-49; the sentences before this one are left as they were written, with their tense pinned, because the argument still holds — but the arity has grown past what they say and the current figures are [ADR-013](ADR-013-feature-flags.md)'s to state, dated, rather than a fifth place to drift). See [ADR-024](ADR-024-trick-play-persistence-boundary.md) for the boundary those two ports draw, and the reversal and the remaining deferrals in the ADR |
| [024](ADR-024-trick-play-persistence-boundary.md) | One adapter implements both trick-play ports, and neither port authorises anybody | Accepted | Yes — `TrickPlayRepositoryAdapter` implements `HandRepository` and `TrickRepository` as one class in EOP-14 Slice C1, because playing a card deletes a `hand_card` row and inserts a `trick_play` row with its components and those writes must commit together; the ports stay separate because their callers are separate. Neither port takes an acting player, so no implementation of them can authorise a requester: `recordDeal` and `appendPlay` check the row, `openTrick` and `recordResolution` check nothing, and the four reads have no parameter to check against. Authorising the requester is the use case's obligation and is **discharged as of EOP-14 Slice C2**: `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase` each resolve the caller through `ResolvePlayerUseCase` as their first port call — the ordinal was "first statement" until EOP-49 re-derived it and found a null check and a session-id read ahead of the call in `PlayCardUseCase`, which changes nothing about the property, since authorisation still precedes every hand, trick and card read — and `PlayCardCommand` carries no seat, no player identifier, no suit and no rank, so a caller cannot name a seat at all. No port can enforce this and no C1 test failed for its absence, which is why it was recorded here rather than only in javadoc; what holds it now is a test per use case asserting that a stranger is refused **before any hand is read**. The oracle described above is therefore reachable only by a member — and reachable in earnest, not latently: `eop.features.trick-play` was `false` when this row was written, gating the beans out of the context, but EOP-70 flipped it `true` on 2026-08-17, so the gate is open and only the authorisation above stands between a requester and the oracle (noted 2026-08-21, EOP-49; the flag's current value and arity live in [ADR-013](ADR-013-feature-flags.md)) |
| [025](ADR-025-dealing-is-its-own-use-case.md) | Dealing is its own use case rather than a step in starting a session, and the deal follows the start write across a seam the ADR argues is chosen rather than forced | Accepted (decided 2026-08-13; both handoffs it deferred discharged 2026-08-14 — Slice D minted the three event names, Slice E published them and deleted the `nextLeaderSeat` placeholder) | Yes — EOP-14 Slice C2 ships `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase` as plain classes in `org.maglez.eop.usecase`, wired in `UseCaseConfiguration` behind `@ConditionalOnProperty` on `eop.features.trick-play`, which `application.yml` defaulted to false when this row was written; `StartSessionUseCase` is untouched and still deals nothing. Authorisation is the first port call of all three, `DealHandsUseCase` returns `void` and refuses a participant with `NotFacilitatorException` and a short table with `TooFewPlayersException` before the deck is even read, and `ResolveTrickUseCase` admits any seated member rather than the facilitator alone. The `DeckShuffler` port with `SecureRandomDeckShuffler` supplies the shuffle `Hands.deal` deliberately omits, and `CardRepository.findWholeDeck()` returns the whole deck in canonical order rather than a page. `TrickPlayDisabledIntegrationTest` asserts all three beans are absent with the flag off while the ungated shuffler remains (noted 2026-08-21, EOP-49: the flag is `true` — EOP-70 set it on 2026-08-17 — so these beans exist and their routes are served; that test now covers ten beans over seven routes, and the live value and arity are [ADR-013](ADR-013-feature-flags.md)'s to state, dated). Two things were deliberately not in Slice C2: no route reached these beans, so the started-but-undealt window of decision 2 was not yet recoverable by a client, and recognising the end of a hand — where `ResolveTrickUseCase` currently writes the winning seat as a placeholder next leader — is Slice E's. EOP-14 Slice D closed the first of the two: `TrickController` now reaches all three beans plus the eleventh use case `ReadOwnHandUseCase`, so a facilitator can deal and a player can read their own hand. The second is closed by EOP-14 Slice E: the placeholder is gone, `TrickRepository.recordResolution` takes an `OptionalInt` next leader and records no leading seat once no seat holds a card, `HandCompleteException` refuses a play into a spent hand with its own 409 rather than the 422 an empty hand would otherwise produce, and `handComplete` on the new state-of-play read says the hand is over without claiming the score is final |
| [026](ADR-026-use-case-observability.md) | Where use case observability lives — option 4 chosen (EOP-70): audit logging at the HTTP boundary in `TrickController` | Accepted (2026-08-17) | Yes — EOP-70 discharged this ADR by choosing option 4: audit logging at the HTTP boundary in `TrickController`. Each of the three game-affecting write endpoints emits one `INFO` line naming the session UUID and the first 8 characters of the actor's token; card identities are never logged (CWE-117 and hidden-information constraints). Two use cases (`ResolveTrickUseCase`, `SweepExpiredSessionsUseCase`) carry pre-existing SLF4J imports from earlier stories — accepted debt. This choice is adequate while every use case is reached through HTTP. |
| [027](ADR-027-singleton-subresource-naming.md) | A singleton sub-resource may be singular, and `/hands` is forbidden rather than merely unused | Accepted | Yes — EOP-14 Slice D ships `GET /api/v1/sessions/{sessionId}/hand`, singular, departing from `api-design.md`'s binding plural-noun rule because the path resolves its subject entirely from the identity token and takes no selector, so it is a singleton by construction rather than a collection that happens to hold one member. The substantive half of the decision is the prohibition, not the naming: no route, use case, port method or DTO may return a hand the caller does not hold, so there is no `HandsDto`, no `HandPage` and no `/hands`. Nothing mechanically enforces that — the way to enforce "this route must never exist" is a review — but adding one would need a new port method or use case, both conspicuous in a diff. `api-design.md` is unamended and plural remains the default; this is a named exception with a stated test. Recorded because @architecture-guardian ruled during the Slice D review that a javadoc is invisible to the next agent, who reads the rule file rather than the controller |
| [028](ADR-028-end-of-hand-without-release-or-score.md) | The end of a hand is reported, not released and not scored | Accepted (amended 2026-08-15 — automatic `COMPLETED` transition added by EOP-15 Slice C) | Yes
| [029](ADR-029-global-rule-loading.md) | All rule files loaded globally via a single glob | Accepted | Yes — `.opencode/opencode.json` `instructions` is `[".opencode/rules/*.md"]`; nine `## Required Reading` sections deleted from agent definitions (EOP-000) — EOP-14 Slice E reports the end of a hand in two derived places and stores it in neither: `current_leader_seat` becomes `NULL` once no seat holds a card, and `handComplete` on `GET /api/v1/sessions/{sessionId}/tricks/current` is computed from `Hands.allEmpty()`. The session status transitions to `COMPLETED` automatically when the last trick resolves (EOP-15 Slice C, ADR-032), or earlier if the facilitator calls `POST /api/v1/sessions/{sessionId}/end`. The second decision is a deliberate non-change: `eop.features.trick-play` stayed `false` on merge, so all five slices shipped unreleased and turning the flag on was its own story whose predecessors were [ADR-026](ADR-026-use-case-observability.md), EOP-48 and EOP-15 — that story ran: EOP-70 flipped the flag `true` on 2026-08-17, all three predecessors are discharged, and the five slices are released (noted 2026-08-21, EOP-49; the flag's live value is [ADR-013](ADR-013-feature-flags.md)'s to state) |
| [030](ADR-030-scoring-is-derived-not-accumulated.md) | The score is derived from play, not accumulated alongside it | Accepted | Yes — EOP-15 Slice A ships `ScoredPlay`, `Standing` and `ScoreSheet` as pure domain types with zero framework imports, and `ScoreSheet.of(players, tricks)` recomputes every figure from the plays and each trick’s winning play rather than reading a stored total. There is no counter column, no increment on the play path and no schema change in this slice, because a stored total is a second authority that can disagree with the play that produced it and the disagreement is silent. That is also the fuller reading of the ticket’s "computed and stored server-side": what it protects is authority, and a derivation leaves the client nothing to assert against. Six further decisions ride with it — the shipped rule stays one point for a threat on your own card plus one for the trick and is explicitly not embellished, with the author’s abandoned v0.21 system and the whitepaper’s beginner relaxation recorded as options rather than built; a linked threat that names no component still scores, settling the question `TrickPlay` left open, because the application holds no model of the system and so cannot validate a component name, and making a point turn on unvalidated client free text would return the score to the client by a side door; a row’s `threatPoint` and `trickPoint` stay separate rather than pre-summed so a human reading the Score Card can see why a row scored; ties are shown by competition ranking and never broken, and no tie-break is invented to get wrong later; a display name is never a key, since two players may choose the same one (ADR-015); and an unresolved trick contributes threat points but no trick point, which makes a running score monotone and lets one class serve both the mid-game read and the final standings. Deciding a game has ended is deliberately not this class’s job — the `COMPLETED` transition is the session’s and arrives in the story’s third slice |
| [031](ADR-031-the-score-is-read-through-its-own-route.md) | The score is read through its own route, and a contradicted game is a server fault | Accepted | Yes — `GET /api/v1/sessions/{sessionId}/score` is served by `ScoreController` behind `eop.features.trick-play`, hand-authored into `docs/api/openapi.yml` before the controller existed per ADR-004. `GetScoreUseCase` takes two collaborators, `ResolvePlayerUseCase` and `TrickRepository`: resolving the caller already yields the session and its players, so no second read of the session is needed, and because a token is only ever matched against the players of the session it was presented for, membership is established by that resolution alone and no further seat check is required. The one new port method, `TrickRepository.findTricks(sessionId)`, returns the session's whole history and filters nothing — an unresolved trick comes back unresolved, because whether a trick is finished is a question the trick answers about itself, and its plays have already scored their threats. It takes no acting player, as ADR-024 requires. Its adapter reads four queries for the whole session rather than three per trick, which at three players is four round trips instead of `1 + 3 × 23` = seventy. There is deliberately no `409`: before the deal the honest answer is everybody on nothing, so unlike the state-of-play read this route needs neither `HandRepository` nor a status check. ADR-027's prohibition is answered in writing rather than assumed — it forbids exposing every player's *private* state through one route, and a score names only cards already face up, which is what separates it from `/hand`. Slice A's eight `IllegalArgumentException` refusals became one `ScoreNotDerivableException` carrying a typed `Reason`, mapped to 500 with a body byte-identical to the no-tampering-card fault, so no identifier can be echoed into a Problem Details response; the four remaining guards interpolate only bounded integers and stay `IllegalArgumentException`. 989 tests (962 before), SpotBugs 0, Checkstyle 0, JaCoCo met over 119 production classes (112 before) with all seven new types at 100% instruction coverage and no branches, and ADR-006's reserved 70% branch minimum restored in `pom.xml` while the bundle measures 90.19%. The flag stayed `false` while these slices shipped and slice C owned the `COMPLETED` transition; both are spent — slice C closed the transition and EOP-70 flipped the flag `true` on 2026-08-17, so `/score` is reachable (noted 2026-08-21, EOP-49; the flag's live value is [ADR-013](ADR-013-feature-flags.md)'s to state). |
| [032](ADR-032-end-of-game-transitions.md) | End-of-game transitions | Accepted | Yes — `GameSession.complete()` and `SessionRepository.recordCompleted()` ship in EOP-15 Slice C; auto-complete fires in `ResolveTrickUseCase` when the last trick resolves, and `POST /api/v1/sessions/{sessionId}/end` provides facilitator early end, both gated on `eop.features.trick-play`. `GAME_COMPLETED` event added to `SessionEventType`. No new DB column — `updated_at` serves as completion time. `SessionNotInProgressException` → 409 in `GlobalExceptionHandler`. |
| [033](ADR-033-session-creation-rate-limit-and-body-size-cap.md) | Session creation rate limit and request body size cap | Accepted | Yes — `SessionCreationLimiter` port and `InMemorySessionCreationLimiter` adapter (EOP-19); reserve-before-work pattern; `RateLimitedException`; `TrustedProxyProperties.sessionCreationLimit`; Caddy `request_body { max_size 16KB }` on `/api/*`; no `max-swallow-size` — considered and deliberately not added (would misrepresent a non-existent second layer). |
| [034](ADR-034-sse-subscriber-cap-and-emitter-timeout.md) | SSE subscriber cap, emitter timeout, and heartbeat stall fix | Accepted | Yes — per-session cap 12 (2× MAXIMUM_PLAYERS), global cap 500, 10-minute emitter timeout, bounded send thread pool replacing single-threaded heartbeat sweep, `spring.mvc.async.request-timeout: 600000` (EOP-20). |
| [035](ADR-035-tls-and-security-response-headers.md) | TLS at Caddy and security response headers | Accepted | Yes — `tls internal` in the Caddyfile, host port mapping changed from `80:8080` to `443:8080`, eight security response headers (CSP, X-Content-Type-Options, Referrer-Policy, HSTS, Permissions-Policy, and — added 2026-08-20 — Cross-Origin-Opener-Policy, Cross-Origin-Resource-Policy, Cross-Origin-Embedder-Policy) applied globally, `Server` header removed, token-storage constraint formalised (EOP-21); site address widened to the loopback IP literals so they no longer bypass the header block, and `*.map` refused at the edge (EOP-107). |
| [036](ADR-036-session-expiry-and-sweep.md) | Session expiry (24-hour TTL) and abandoned-session sweep | Accepted (EOP-22, 2026-08-16) | Yes — `expires_at` column added by Liquibase changeset `006`, expiry guard in `ResolvePlayerUseCase` (ungated), `SweepExpiredSessionsUseCase` + `ExpiredSessionSweepScheduler` gated on `eop.features.session-lifecycle`. Reverses ADR-019's "no session expiry" consequence. |
| [037](ADR-037-frontend-build-time-feature-flags.md) | Front-end feature flags are build-time Vite variables, not runtime properties | Accepted (EOP-11, 2026-08-16) | Yes — `VITE_GAME_SCREEN_ENABLED` is the live instance of this pattern (EOP-77, 2026-08-18 amended; `VITE_LOBBY_UI_ENABLED` was retired once the lobby UI was stable). `VITE_GAME_SCREEN_ENABLED` is declared in `ui/src/vite-env.d.ts` and read in `App.tsx` (the view router) as `import.meta.env.VITE_GAME_SCREEN_ENABLED === 'true'`, so an unset flag fails closed. Both positions (flag on and flag off) are tested via `vi.stubEnv` in `App.test.tsx`. |
| [038](ADR-038-pointer-event-drag-and-drop.md) | Pointer-Event drag-and-drop for card play, with no DnD library | Accepted (EOP-60, 2026-08-16) | Yes — `GameScreen.tsx` uses `onPointerDown`/`onPointerMove`/`onPointerUp`/`onPointerCancel` with `setPointerCapture` and `getBoundingClientRect` drop detection. Drag is an enhancement: every card is a `role="button"` with keyboard handler and a "Play selected card" button provides the WCAG 2.2 AA keyboard path. `VITE_GAME_SCREEN_ENABLED` declared in `vite-env.d.ts`, off by default. Per the EOP-79 amendment, no descendant of the drag surface may be natively draggable — all three card `<img>` elements (hand card, trick-zone played card, drag ghost) carry `draggable={false}`, because a native HTML5 drag fires `pointercancel` and aborts the gesture; the ghost does not strictly need it, its wrapper having `pointerEvents: 'none'`, but is guarded so the invariant holds uniformly. `handlePointerDown` also cancels the `pointerdown` default action, so a mouse press no longer moves focus. The amendment records that jsdom cannot reproduce a native drag, so the two `draggable="false"` assertions on the hand and trick-zone images are the only automated guards on that regression; the ghost is guarded but unasserted. |
| [039](ADR-039-new-game-reset.md) | New-game reset: COMPLETED → IN_PROGRESS via non-atomic multi-step sequence | Accepted (EOP-65, 2026-08-16) | Yes — `NewGameUseCase` clears tricks then hands, resets session via CAS, re-deals. `PersistGameResultUseCase` called best-effort from `ResolveTrickUseCase` via `Optional`. |
| [040](ADR-040-trick-play-flag-on.md) | Enable `eop.features.trick-play` (EOP-70) | Accepted (2026-08-17) | Yes — `trick-play: true` in `application.yml`; ADR-026 discharged (option 4: audit logging at HTTP boundary in `TrickController`); UI wires `POST /deal` after `POST /start`; `canStartGame >= 3`; `GameScreen` distinguishes 409 (HandNotDealtException) from 404 (session gone). |
| [041](ADR-041-printed-deck-has-no-aces.md) | Printed deck has no Ace cards — deck size is 68, King is highest (EOP-75) | Accepted (2026-08-18) | Yes — `Rank.ACE` removed, `Card.isOpenThreat()` removed, migration `2026-08-18--remove-ace-cards.xml` deletes 6 Ace rows. |
| [042](ADR-042-game-over-flag-on.md) | Enable `eop.features.game-over`, and the shipped-flag-default trap (EOP-82) | Accepted (2026-08-18) | Yes — `game-over: true` in `application.yml`; `GameOverController` and the three use-case beans are live, so leaderboard refusals are RFC 9457 again and `game_result` rows are written. `GameOverScreen` ejects on 403 only, with a `Retry loading results` button on 404. `ShippedFeatureFlagDefaultsTest` pins the three shipped flag defaults by reading `application.yml` off the classpath — recorded as an interim workaround, not the tripwire EOP-27 built. Expiry condition for the flag is dated, not open-ended. |
| [043](ADR-043-liquibase-contexts-are-not-used.md) | Liquibase contexts are not used — a bare `context="prod"` restricts nothing unless `spring.liquibase.contexts` names a real, non-empty context in every profile, verified across 21 YAML encodings (EOP-35) | Accepted (2026-08-19) | Yes — no changeset carries a `context`, the property is unset, `.opencode/rules/database.md` keeps the directive, and `LiquibaseContextGatingAbsentTest` fails the build on either half |
| [044](ADR-044-changelog-file-naming-is-dated.md) | New changelog files are named `YYYY-MM-DD--<description>.xml` and the legacy `NNN-` sequence is frozen at `006-`, because `<includeAll>` orders by filename and a `007-` file would execute ahead of all three dated migrations (EOP-36) | Accepted (2026-08-19) | Yes — `.opencode/rules/database.md` carries the directive and the one-line reason; the ordering proof, the frozen-identity argument and the `classpath:` history live in the ADR. Enforced by review only — no test asserts the scheme, deliberately. |
| [045](ADR-045-frontend-response-validation.md) | Response DTOs are parsed at the browser boundary by hand-written per-DTO parsers, and a contract violation is a 502 `ContractViolationError` (EOP-108) | Accepted (amended 2026-08-19) | Yes — the ten JSON-returning helpers in `ui/src/api.ts` parse instead of asserting, the four `is*` guards from EOP-105 gain production call sites, and both fields whose contract schema `$ref`s the `StrideCategory` mirror are narrowed to it: `CardDto.suit` by EOP-108 and `TrickDto.ledSuit` by EOP-109, the latter through an `optionalEnum` helper so absence stays absence. `Card.rank`/`CardDto.rank` remain bare `string` against a `$ref` to `Rank`, deliberately — see §4. Strictness is bounded to structure and enum membership, not value formats. Parser field coverage is enforced by review only — no test detects a field added to a DTO but not to its parser. |
| [046](ADR-046-gate-model-capability-floor.md) | A Definition-of-Done gate is pinned by a capability floor and a passing probe, not by tier name (EOP-000) | Accepted | Yes — rule 1 of ADR-022 is superseded: a gate tier must be reasoning-capable, emit structured tool calls, carry at least 40,000 max output tokens and at least 128,000 context, be family-independent of every tier that authored the artefact it reviews, and have PASSED the two-stage probe recorded in Blueprint §3.4.1. `MODEL_F` is created as `amazon-bedrock/minimax.minimax-m2.5`, holding `@architecture-guardian` and `@security-auditor`, which closes the second exception to the Separation Invariant. The probe is mandatory because three candidates satisfying every metadata clause on paper proved unusable — one emitted an unparseable reasoning stream, one fabricated a file read, and one answered correctly without calling the tool at all. Nothing enforces any clause programmatically, so the wording is the mechanism; `MODEL_C`/`MODEL_E` stay ineligible as a consequence of the floor rather than by name |
| [047](ADR-047-h2-excluded-from-deployable-artifact.md) | H2 is excluded from the deployable artifact rather than moved to test scope, so local development keeps its in-memory database while the jar that ships carries no web console (EOP-34) | Accepted | Yes — `spring-boot-maven-plugin`'s `repackage` execution excludes `com.h2database:h2`, removing `BOOT-INF/lib/h2-2.4.240.jar` and its 13 `org.h2.server.web` classes from the artifact; the 2,685,418-byte nested jar is gone. The scope stays `runtime` deliberately, because the default profile resolves `org.h2.Driver` and ADR-012 makes `./mvnw spring-boot:run` a decision rather than an accident — proven unbroken by starting it and getting a 200 from `/health`. Gated by `tools/artifact/assert-no-h2-in-jar.sh` in the `build` job, which no *surefire* test could do since surefire runs before `package` — a failsafe IT could, and was rejected only because declaring one would make a seventh declared Maven plugin; proven non-vacuous by restoring `origin/main`'s `pom.xml` and watching it fail. EOP-34's second item, a `maven-enforcer` ban on the console module, is **declined** — a ban with no sanctioned escape hatch trains people to disable bans, and the EOP-27 tripwire is the satisfiable control for the same risk. Two corrections recorded: `runtime` scope already kept `JakartaWebServlet` off the compile classpath, so the hand-registration path needs reflection or a second reviewed change rather than "zero dependency changes"; and a laptop still has H2 on its runtime classpath, so this gates the artifact and not the workstation |
| [048](ADR-048-narrow-exception-handler-to-domain-type.md) | Narrow the IllegalArgumentException handler to a domain type, preserving persistence exception translation (EOP-28) | Accepted | Yes — `InvalidInputException` minted as a subclass of `IllegalArgumentException`, the handler narrowed to it, caller-facing guards converted (PageQuery, DisplayName, TrickPlay), internal guards left as JDK type and now become logged 500s, and `GlobalExceptionHandlerTest` gains a reflection test asserting no JDK type appears in any `@ExceptionHandler` except the catch-all. The subclass is load-bearing because Spring's persistence exception translation rewrites a bare `IllegalArgumentException` from a `@Repository` into `InvalidDataAccessApiUsageException`, preserving the 500-for-corrupt-row behaviour; extending `RuntimeException` would have escaped translation and echoed a database error to the caller as a 400 |

The "Implemented?" column exists because an accepted ADR is a decision, not a
delivery. `CHANGELOG.md` separates the same two things for the same reason.

## Why numbering starts at 002

There is no ADR-001. The first architectural decision — adopting Spring Boot as the
application framework — was recorded as ADR-002 and 001 was never written. Rather
than renumber a set of documents that other files reference by number, the gap is
recorded here and left alone.

## Adding an ADR

1. Take the next free number. Do not reuse a number, even for a superseded ADR.
2. Name the file `ADR-NNN-kebab-case-title.md`.
3. Follow the house structure: `# ADR-NNN: Title`, then `**Status:**`, `**Date:**`
   and `**Deciders:**`, then `## Context`, `## Decision`, `## Consequences`,
   `## Related`.
4. State consequences honestly, including the negative ones. An ADR that lists only
   benefits has not recorded a decision, only an advertisement.
5. Add a row to the index above, and keep its Status cell in step with the ADR's own
   `**Status:**` line — including the date whenever an amendment changes the status.
   An amendment that leaves the status word alone, such as withdrawing a consequence
   that was never true, needs no date in either place; that is why several ADRs here
   record a dated amendment — some under a `## Amendments` heading, one as an inline
   `**Amendment, YYYY-MM-DD …**` paragraph — above a bare `Accepted`.
   `AdrIndexConsistencyTest` fails the build if a file has no row, a row points at no
   file, a file carries anything other than exactly one `**Status:**` line, the leading
   status word disagrees, or a dated status line reaches the index without its date.
   It checks that last rule in one direction only — an undated status line is always
   accepted. That test exists because this step was drifted past twice.
6. To reverse a decision, write a new ADR that supersedes the old one and mark the
   old one `Superseded by ADR-NNN`. Do not edit the original's decision.
